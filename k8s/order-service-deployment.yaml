apiVersion: apps/v1
kind: Deployment
metadata:
  name: agri-order-service
  labels:
    app: agri-order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agri-order-service
  template:
    metadata:
      labels:
        app: agri-order-service
      annotations:
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "agri-backend-role"
        vault.hashicorp.com/agent-inject-secret-application.properties: "secret/data/agri/db"
        vault.hashicorp.com/agent-inject-template-application.properties: |
          {{- with secret "secret/data/agri/db" -}}
          spring.data.mongodb.uri=mongodb://{{ .Data.data.username }}:{{ .Data.data.password }}@agri-mongodb:27017/marketplace_order?authSource=admin
          server.port=8083
          cors.allowed.origins=*
          product-service.url=http://agri-product-service:8082
          auth-service.url=http://agri-auth-service:8081
          {{- end }}
    spec:
      serviceAccountName: agri-backend-sa
      containers:
      - name: agri-order-service
        image: agri-order-service:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: SPRING_CONFIG_ADDITIONAL_LOCATION
          value: "file:/vault/secrets/application.properties"
        ports:
        - containerPort: 8083
---
apiVersion: v1
kind: Service
metadata:
  name: agri-order-service
spec:
  selector:
    app: agri-order-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP